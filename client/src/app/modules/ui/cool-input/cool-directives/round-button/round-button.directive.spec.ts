import { RoundButtonDirective } from './round-button.directive';

describe('RoundButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundButtonDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
